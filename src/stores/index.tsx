import { ContextProvider } from "./providerCompose";
import { connectionContext as ConnectionContext } from "./connection";
import { RoomProvider } from "./room";

// Export files in a pretty way
export { ConnectionContext, RoomProvider };

export default ContextProvider;
